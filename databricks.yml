bundle:
  name: oc_dab

# Define the Custom Variable at the Top Level
variables:
  config_file_path:
    description: "Path to the Environment Variable File for Top Level Inclusions for Safe Default Fallback"
    default: "variables/dev_var.yml"

include:
  - resources/jobs/*.yml
  - variables/*.yml
  - configs/*.yml